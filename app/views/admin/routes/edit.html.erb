<h1>Editing Route</h1>

<div class="row">
  <div class="col-md-3">
    <%= render 'form', route: @route %>
  </div>
</div>

<%= link_to 'Show', [:admin, @route], class: 'btn btn-success' %> |
<%= link_to 'Back', admin_routes_path, class: 'btn btn-warning' %>
